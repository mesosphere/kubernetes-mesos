  {
    "id": "redis-slave-controller",
    "kind": "ReplicationController",
    "apiVersion": "v1beta1",
    "desiredState": {
      "replicas": 2,
      "replicaSelector": {"name": "redis-slave"},
      "podTemplate": {
        "desiredState": {
           "manifest": {
             "version": "v1beta1",
             "id": "redis-slave-controller",
             "containers": [{
               "name": "redis-slave",
               "image": "mesosphere/kubernetes:guestbook-redis-slave-5",
               "ports": [{"containerPort": 6379}],
               "env": [{"key": "REDIS_MASTER_SERVICE_HOST", "value": "redis-master.default.kubernetes.local"}]
             }]
           }
         },
         "labels": {"name": "redis-slave"}
        }},
    "labels": {"name": "redis-slave"}
  }
