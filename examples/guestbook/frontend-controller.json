  {
    "id": "frontend-controller",
    "kind": "ReplicationController",
    "apiVersion": "v1beta1",
    "desiredState": {
      "replicas": 3,
      "replicaSelector": {"name": "frontend"},
      "podTemplate": {
        "desiredState": {
           "manifest": {
             "version": "v1beta1",
             "id": "frontend-controller",
             "containers": [{
               "name": "guestbook-frontend",
               "image": "mesosphere/kubernetes:guestbook-frontend-php-5",
               "ports": [{"containerPort": 80}],
               "env": [{"key": "REDIS_MASTER_HOSTNAME", "value": "redis-master.default.kubernetes.local"}]
             }]
           }
         },
         "labels": {"name": "frontend"}
        }},
    "labels": {"name": "frontend"}
  }
